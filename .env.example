# For production (Vercel): set GH_TOKEN; repo is auto-detected from the deployment. Optional: GH_PATH, GH_BRANCH, GH_REPO.
# For local: omit GH_TOKEN to use data/readings.json on disk, or set GH_TOKEN + GH_REPO to use GitHub.
GH_TOKEN=
